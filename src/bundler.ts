import { Bundle, DataItem, createData } from "arbundles/node";


export default class Bundler {

  async bundle(data: Buffer): Promise<DataItem> {
    throw Error();
    // let result: DataItem = createData();
    // return result;
  }
}